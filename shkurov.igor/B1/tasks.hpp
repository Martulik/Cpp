#ifndef TASKS_HPP
#define TASKS_HPP

namespace shkurov
{
  void taskOne(const char* cmp);
  void taskTwo(const char* filename);
  void taskThree();
  void taskFour(const char* cmp, int size);
}

#endif
